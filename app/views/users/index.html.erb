<%# <div class="wrapper d-flex flex-column justify-content-between"> %>
  <%# <div class="container">
    <div class="content-aligned pt-3">
    <h1>GymBuddy</h1>
    </div> %>
    <div data-controller="slide">
      <button data-action="click->slide#slideup" class="controls" id="previous">No</button>
      <button data-action="click->slide#slidedown" class="controls" id="next">Yes</button>
      <div>
        <ul id = "slides" data-slide-target="slider">
          <% @users_matching.each do |user| %>
         <div class= "slide">
            <li><div class="card-category my-4 mx-auto" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path user.photo.key%>');">
              <div class=" d-flex flex-gap">
                <div class="name pt-5" style= "font-weight: bolder; color: white">
                  <h2>
                    <%= user.first_name %>, <%= user.age %>
                  </h2>
                </div>
                <div class="card rounded-card" style="width: 18rem; color:black;">
                  <div class="card-body">
                      <p>Gender: <%= user.gender %></p>
                      <p>Level of fitness: <%= user.level_of_fitness%></p>
                      <p>Partner preference: <%= user.partner_gender_preference%></p>
                      <p>Days available: <% user.days_available.each do |day| %>
                        <%= day %>
                      <% end %></p>
                  </div>
                </div>
              </div>
            </div>
          </div></li>
          <% end %>
        </ul>
      </div>

    </div>


      <%# </div> %>
    <%# <%= render "shared/footer" %>
  <%# </div> %>


<%# class = "card " style= "background-color:#D3D1D2; border-radius: 15px; width:90%; color:black" %>
